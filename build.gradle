plugins {
    id 'org.jetbrains.intellij' version '0.0.35'
}

allprojects {

    apply plugin: 'java'

    sourceSets {
        main {
            java.srcDirs 'src', 'gen'
            resources.srcDir 'resources'
        }
        test {
            java.srcDir 'tests'
        }
    }

    apply plugin: "org.jetbrains.intellij"

}

repositories {
    flatDir {
        dirs 'libs'
    }
}


dependencies {
    compile project ('coq-jps-plugin')
}


